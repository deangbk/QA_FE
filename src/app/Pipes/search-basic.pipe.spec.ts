import { SearchBasicPipe } from './search-basic.pipe';

describe('SearchBasicPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBasicPipe();
    expect(pipe).toBeTruthy();
  });
});
